<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GameRec</title>
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"/>
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
    <nav class = "navbar bavbar-expand-lg bg-custom-2 text-light">
      <div class = "btn-group">
        <a class = "btn btn-primary btn-lg" href="/" type="button">Home</a>
        <a class = "btn btn-primary btn-lg" href="#" type="button">Games</a>
        <a class = "btn btn-primary btn-lg" href="#" type="button">About us</a>
      </div>

      <div class= "d-flex">
        <div class="input-group rounded">
          <i class="bi bi-search"></i>
          <input type="search" class="form-control rounded" placeholder="Search" aria-label="Search" aria-describedby="search-addon" />
        </div>    
          {{#if logged_in}}
        <a class = "btn btn-primary btn-lg" href="/profile">Profile</a> |
        <a class="btn btn-primary btn-lg" id="logout" href="/home">Logout</a>
        {{else}}
        <a class = "btn btn-primary btn-lg" href="/login" type="button">Login</a>
        {{/if}}
      </div>
    </nav>  
    
  </header>

    <main class="container container-fluid mt-5">
    <!-- Render the sub layout -->
       {{{ body }}}
    </main>
    
    <footer></footer>
  </div> 

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  
</body>

</html>
